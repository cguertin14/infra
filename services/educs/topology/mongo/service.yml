apiVersion: v1
kind: Service
metadata:
  name: mongo
spec:
  ipFamilyPolicy: SingleStack
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 27017
    protocol: TCP
    targetPort: 27017
    name: mongo