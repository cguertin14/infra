resources:
- ../../base
- resources/ingress.yml
- resources/autoscaler.yml

generators:
- secrets-generator.yml

patchesJson6902:
- path: patches/update-resources-deployment.yml
  target:
    group: apps
    kind: Deployment
    name: traefik
    namespace: kube-system 
    version: v1

configMapGenerator:
- name: traefik-forward-auth-config
  namespace: kube-system
  files:
  - traefik-forward-auth.ini=config/traefik-forward-auth.ini