apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-rollouts-dashboard
spec:
  template:
    spec:
      containers:
      - name: argo-rollouts-dashboard
        command:
          - /bin/kubectl-argo-rollouts
          - dashboard
          - --insecure-skip-tls-verify