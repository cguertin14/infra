apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: obsidianls-couchdb
spec:
  selector:
    matchLabels:
      app: couchdb
      release: obsidianls
  endpoints:
  - port: metrics
    path: /_node/_local/_prometheus
    interval: 30s
