apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: velero-s3-credentials
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
- ./secrets/velero-s3-credentials.yml
