apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: oauth-secret-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
- ./secrets/oauth-secrets.yml
